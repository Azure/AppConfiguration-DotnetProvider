namespace Microsoft.Extensions.Configuration.Azconfig.Models
{
    public class LabelFilter
    {
        /// <summary>
        /// The label filter used to filter queried key-values with empty label.
        /// </summary>
        const string Empty = "\0";
    }
}
